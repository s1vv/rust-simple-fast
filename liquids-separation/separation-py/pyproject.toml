[project]
name = "separation-py"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.1",
    "pytest>=8.4.1",
    "pytest-benchmark>=5.1.0",
]
